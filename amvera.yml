meta:
  environment: python
  toolchain:
    name: python
    version: 3.11

build:
  command: pip install --no-cache-dir -r requirements_rag.txt

run:
  command: python src/bot.py
  persistenceMount: /data